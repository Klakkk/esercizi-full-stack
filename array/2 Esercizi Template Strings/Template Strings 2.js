//Utilizza una template string per creare una stringa su più righe che includa interruzioni di riga.

const myString= `questa è stringa multilina
semplificato, ovvero, usando il backstick non ho bisogno di scrivere\n\ per
andare a capo`